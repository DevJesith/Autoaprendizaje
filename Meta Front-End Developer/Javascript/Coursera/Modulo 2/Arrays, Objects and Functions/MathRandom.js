//Generate a decimal numer between 0 and 0.99

Math.random();

//Save it to a variable:
var decimal = Math.random();

//log the value of decimal to the console
console.log(decimal);

//Log the value of decimal MILTIPLIED BY 10
console.log(decimal * 10);